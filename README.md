# ForwardAutodiff_overloading
This is a simple implementation of Multivariable Forward Automatic Differentiation, which uses Operator Overloading method. In each call, a single calculaion graph is built for each statement so that the derivative can be evaluated for multiple set of values by just one object of calculation tree.
